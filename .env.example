# Copy this file to .env and fill in real values. .env is gitignored.

# Hugging Face Hub (https://huggingface.co/settings/tokens)
HF_TOKEN=hf_your_token_here
HF_USERNAME=your_hf_username
HF_MODEL_REPO=fraud-detection-models

# Kaggle (https://www.kaggle.com/settings -> Create New Token)
KAGGLE_USERNAME=your_kaggle_username
KAGGLE_KEY=your_kaggle_api_key

# FraudGuard inference API (the HF Space deployed by scripts/push_api_to_hf.sh)
# If set, the GUI sends predictions over HTTPS to this URL.
# If unset/empty, the GUI falls back to downloading models from HF Hub and predicting locally.
FRAUDGUARD_API_URL=https://your_hf_username-fraudguard-api.hf.space
FRAUDGUARD_API_TIMEOUT=30
