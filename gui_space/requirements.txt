# Streamlit + visualization
streamlit>=1.30.0
streamlit-option-menu>=0.3.12
plotly>=5.17.0

# Data (used by Data Explorer page reading from HF dataset repo)
pandas>=2.0.0
numpy>=1.24.0

# HF + HTTP API
huggingface_hub>=0.20.0
requests>=2.31.0

# Inference fallback (only used if FRAUDGUARD_API_URL is unset)
scikit-learn>=1.3.0
imbalanced-learn>=0.11.0
joblib>=1.3.0

# Env
python-dotenv>=1.0.0
